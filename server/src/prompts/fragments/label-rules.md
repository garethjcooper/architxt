### Label and detail rules

- `label` is a short human-readable phrase, maximum 4 words, that describes the relationship.
- `detail` is the full description of the edge context, maximum 4 sentences. Use an empty string if no detail is justified.
